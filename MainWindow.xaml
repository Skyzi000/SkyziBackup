<Window x:Class="SkyziBackup.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SkyziBackup"
        mc:Ignorable="d"
        Title="SkyziBackup" Height="450" Width="800">
    <Grid Margin="10,18,10,5">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition MaxHeight="150" Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition MaxWidth="500" Width="*"/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel Margin="10,0" Grid.RowSpan="2" Grid.ColumnSpan="3">
            <Label x:Name="originPathLabel" Content="バックアップ元" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            <TextBox x:Name="originPath" HorizontalAlignment="Stretch" TextWrapping="NoWrap" VerticalAlignment="Top"/>
            <Label x:Name="destPathLabel" Content="バックアップ先" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            <TextBox x:Name="destPath" HorizontalAlignment="Stretch" TextWrapping="NoWrap" VerticalAlignment="Top"/>
            <Label x:Name="passwordLabel" Content="パスワード" HorizontalAlignment="Left" VerticalAlignment="Top"/>
            <PasswordBox x:Name="password" HorizontalAlignment="Stretch" VerticalAlignment="Top" ToolTip="空欄の場合は暗号化しません。オプションからパスワードを保存するかどうか設定できます。"/>
            <Button x:Name="StartBackupButton" Click="StartBackupButton_ClickAsync" Content="バックアップ開始" Height="30" HorizontalAlignment="Stretch" VerticalAlignment="Top" Grid.ColumnSpan="3" Grid.Row="1" Margin="10" FontSize="20"/>
            <ProgressBar x:Name="progressBar" Height="30" Margin="10,-40,10,10" IsIndeterminate="True" Grid.ColumnSpan="2" Visibility="Collapsed" VerticalAlignment="Top" />
        </StackPanel>
        <GroupBox Header="メッセージ" Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="3">
            <ScrollViewer Name="MessageScrollViewer" VerticalScrollBarVisibility="Auto" BorderBrush="Black">
                <TextBlock x:Name="message" HorizontalAlignment="Left" Text="" TextWrapping="Wrap" VerticalAlignment="Top"/>
            </ScrollViewer>
        </GroupBox>
        

        <Menu DockPanel.Dock="Top" Height="18" VerticalAlignment="Top" Margin="-10,-18,-10,0" Grid.ColumnSpan="3">
            <MenuItem Header="ファイル(_F)">
                <MenuItem Header="最新のログファイルを開く(_L)" Click="OpenLog_Click"/>
                <MenuItem Name="CloseButton" Header="閉じる(_X)" Click="CloseButton_Click"/>
                <Separator/>
                <MenuItem Header="終了(_E)" Click="Exit_Click"/>
            </MenuItem>
            <MenuItem Header="表示(_V)">
                <MenuItem Name="RestoreWindowMenu" Header="リストア(復元)ウィンドウ" Click="RestoreWindowMenu_Click"/>
            </MenuItem>
            <MenuItem Header="オプション(_O)">
                <MenuItem x:Name="GlobalSettingsMenu" Header="グローバル設定(_G)" Click="GlobalSettingsMenu_Click"/>
                <MenuItem x:Name="LocalSettingsMenu" Header="ローカル設定(_L)" Click="LocalSettingsMenu_Click"/>
                <MenuItem Name="ShowCurrentSettings" Header="現在の設定を表示" Click="ShowCurrentSettings_Click"/>
                <Separator/>
                <MenuItem x:Name="DeleteLocalSettings" Header="ローカル設定を削除" Click="DeleteLocalSettings_Click"/>
            </MenuItem>
        </Menu>
    </Grid>
</Window>
